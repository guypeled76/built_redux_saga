name: redux_saga
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: guy.p <email@example.com>

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  rxdart: ^0.20.0
  built_redux_rx:
    git: https://github.com/guypeled76/built_redux_rx
  built_redux:
    git: https://github.com/guypeled76/built_redux

dev_dependencies:
  build_runner: ^1.0.0
  built_value_generator: ^6.3.0
  json_serializable: ^2.0.1
  pedantic: ^1.0.0
  test: ^1.0.0
